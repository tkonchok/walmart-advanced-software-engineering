# Power-of-Two Max Heap

Status: In progress

This project implements an array-backed max heap whose branching factor is a power of two.

## Project Structure

- `src/` — heap implementation
- `test/` — correctness tests
- `benchmark/` — performance benchmarks